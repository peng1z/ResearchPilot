# Runtime dependencies are declared in pyproject.toml. This file exists only
# so the Docker build has something to install. Deriving it from pyproject
# instead of restating the list means the two cannot drift apart -- they had,
# on both qdrant-client's version bound and sentence-transformers.
.[local-embeddings]
