# Loaded by `next dev` only. docker-compose sets this for the container build;
# the static demo build deliberately leaves it unset, so the deployed page
# makes no backend requests.
NEXT_PUBLIC_API_BASE=http://localhost:8000
